export const API_BASE_URL = import.meta.env.VITE_API_BASE_URL || "/api";
// Central API base
// In production (Vercel), default to '/api' and use vercel.json rewrites.
// In local dev, Vite proxy will map '/api' to the backend.
